import InteractiveClient from "./interactive-client";

export const metadata = {
    title: "Interactive WhatsApp Messages | sole-what",
    description: "Send WhatsApp Polls, Live Location Pins, and Contact Cards (VCards) with real-time previews.",
};

export default function InteractivePage() {
    return <InteractiveClient />;
}
